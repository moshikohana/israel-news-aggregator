# 🦒 AR חיות בגודל אמיתי

פיצ'ר מציאות רבודה שמציב חיות תלת-ממד **בגודלן האמיתי** בתוך פריים המצלמה החי
של המכשיר. נמצא בנתיב `/ar` (קישור גם מעמוד הבית).

## איך זה עובד

| | מצב AR מלא (WebXR) | מצב מצלמה |
|---|---|---|
| מכשירים | אנדרואיד / Chrome, משקפי AR | iOS Safari וכל דפדפן עם מצלמה |
| רצפה | זיהוי משטח אמיתי (`hit-test`) - לוחצים והחיה נוחתת על הרצפה | רצפה מונחת בגובה 1.6 מ' מתחת לעין |
| מבט | מעקב מיקום ותנועה מלא של המכשיר | חיישני `deviceorientation` (ג'ירוסקופ) |
| קנה מידה | מדויק אוטומטית | תלוי בכיול שדה הראייה (מחוון ⚙️) |

יש גם מצב "תצוגה מקדימה" ללא מצלמה - שימושי במחשב ובבדיקות.

## קנה מידה אמיתי

יחידה אחת בסצנה = מטר אחד בעולם. כל מודל מנורמל בזמן ריצה לגובה ולאורך
האמיתיים של החיה (`heightM`, `lengthM` ב-`static/js/animals.js`) ומוצמד לרצפה,
כך שפיל יוצא 3.3 מ' גובה ו-6.5 מ' אורך - בדיוק כמו במציאות.
כפתור 🧍 מוסיף דמות אדם בגובה 1.75 מ' להשוואה.

## המודלים

החיות נבנות פרוצדורלית מפרימיטיבים של three.js (קפסולות, כדורים, קונוסים)
עם טקסטורות שנוצרות ב-canvas (פסים של זברה, כתמי ג'ירפה, פרוות רעמה).
אין קבצי GLB חיצוניים ואין CDN - three.js עצמו מוגש מקומית מ-`static/js/vendor/`,
כך שהפיצ'ר עובד גם ברשת סגורה.

17 חיות: פיל, ג'ירפה, אריה, טיגריס, ברדלס, זברה, סוס, פרה, דוב, זאב, כלב,
חתול, קרנף, אייל, פינגווין, קנגורו וטי-רקס.

## קבצים

```
templates/ar_animals.html    עמוד ה-AR (RTL, ממשק בעברית)
static/js/animals.js         קטלוג החיות + בניית המודלים + אנימציה
static/js/ar-animals.js      מנוע ה-AR: WebXR, מצלמה, מגע, צילום מסך
static/css/ar.css            עיצוב הממשק
static/js/vendor/            three.js (MIT)
app.py                       נתיב /ar
```

## הרצה

```bash
pip install flask requests beautifulsoup4
python app.py
```

⚠️ **גישה למצלמה דורשת HTTPS.** בדפדפן `http://localhost` זה עובד,
אבל מכתובת IP ברשת המקומית או בשרת אמיתי צריך תעודת SSL
(למשל `flask run --cert=adhoc`, או פרוקסי עם HTTPS).

הווידאו והעיבוד רצים כולם בדפדפן - שום פריים מהמצלמה לא נשלח לשרת.
