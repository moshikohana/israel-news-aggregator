# תשובה לבקוביץ׳ — סרטון 60 שניות

מענה עובדתי לסרטון שבו יינון בקוביץ׳ מציג מצגת שנועדה להוכיח שנתניהו הוא "הכי שמאלני
מכולם".

**הגישה (גרסה 2):** לא מתווכחים עם השקופיות — מקבלים את כולן, ותוקפים את המסקנה.
הסרטון נפתח ב"בקוביץ׳ צודק. בכל שבע השקופיות. אז איך המסקנה יצאה הפוכה?", מראה מה
שיטת המצגת מסתירה (מה התקבל, מה הותנה, מה לא קרה), ומסיים בשקופית שלא הייתה שם:
מדינה פלסטינית לא קמה.

| קובץ | מה יש בו |
|---|---|
| `brag.mp4` | הסרטון — אנכי 9:16, 60.0 שניות |
| `brag.jpg` | תמונת הכריכה (צרובה גם כ-cover art של ה-MP4) |
| `script.md` | התסריט לפי סצנות, דף עובדות עם מקורות, ומה הושאר בחוץ ולמה |
| `share-copy.txt` | טקסט לפוסט, גרסה קצרה, והערות לפני פרסום |
| `brag-plan.md` | הסטוריבורד והחלטות הטון |
| `composition/` | מקור הסרטון (HTML + GSAP, Hyperframes) |
| `composition/media/` | לכאן נכנסים תמונות וקטעי וידאו — ראו ה-README שם |

## להוסיף תמונות וקטעי וידאו של ביבי

התשתית מוכנה ומחכה לקבצים. שומרים קובץ לפי שנה (`1997.jpg`, `2009.mp4`...) בתוך
`composition/media/`, ומריצים:

```bash
cd composition
node build.mjs          # מזהה את הקבצים ומחבר כל אחד לכרטיס שלו
npx hyperframes check
npx hyperframes render --output ../brag.mp4
```

כל קובץ נכנס כרקע מאחורי הטקסט — שחור-לבן, מוחשך, עם שכבת הצללה — כדי שהכרטיס יישאר
קריא בטלפון. פירוט מלא, כולל מה לחפש לכל שנה: `composition/media/README.md`.

**את החומר עצמו צריך להביא ידנית.** מדיניות הרשת של סביבת העבודה חוסמת את כל מקורות
המדיה (403 על ויקישיתוף, ארכיון האינטרנט, יוטיוב, לשכת העיתונות הממשלתית), ומעבר לזה
צילומי ארכיון מהשנים האלה הם חומר מוגן — ההחלטה לפרסם סרטון פוליטי שבנוי עליהם היא
שלך. השיקולים (שימוש הוגן לצורך ביקורת מול Content ID) מפורטים ב-`media/README.md`.

## לבנות מחדש

```bash
cd composition && node build.mjs && npx hyperframes check && npx hyperframes render --output ../brag.mp4
```

הפונטים (Heebo) ו-GSAP שמורים מקומית תחת `composition/assets/`, כך שהרינדור זהה בכל
הרצה ולא תלוי ברשת.

## שתי הערות לפני פרסום

1. **כספי קטר (2018) לא בסרטון.** הטענה של בקוביץ׳ נכונה, וההגנה המקובלת דורשת מקור
   מסודר. בלי מקור — עדיף בחוץ מאשר בניסוח מעורפל.
2. **הטענה הנגדית שנשארת:** הדפוס. גרסה 2 לא מתחמקת ממנו — היא טוענת שהדפוס הוא
   ויתורים מותנים שלא הבשילו, ומביאה ראיה (וואי הוקפא אחרי 2%, ההקפאה פגה ולא
   הוארכה, המנה הרביעית בוטלה). זה טיעון שאפשר לחלוק עליו, אבל הוא טיעון.

פירוט מלא ב-`script.md`.
